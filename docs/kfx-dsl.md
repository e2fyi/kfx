# Kubeflow Pipeline DSL helper

`kfx.dsl` is a module with helper classes to config some of the more common settings
for the `kfp.dsl.ContainerOp` objects in the pipeline.


::: kfx.dsl:ContainerOpTransform
